<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GymFit — Sign In</title>
    <link rel="stylesheet" href="/gymfit/public/assets/css/style.css">
</head>
<body>

<?php
$old = $data['old'] ?? [];
$errors = $data['errors'] ?? [];
?>

<div class="auth-page">

    <!-- LEFT — desktop only -->
    <div class="auth-left">
        <div class="auth-left-content">
            <p class="auth-left-title">
                Train Hard<em>Live Strong</em>
            </p>
            <ul class="auth-perks">
                <li>Access to 45+ weekly classes</li>
                <li>12 expert certified coaches</li>
                <li>Open 7 days a week</li>
                <li>Premium equipment & facilities</li>
                <li>Sauna & hammam — VIP plan</li>
            </ul>
        </div>
    </div>

    <!-- RIGHT — form -->
    <div class="auth-right">
        <div class="auth-box">

            <a href="index.php?route=home" class="auth-logo">
                Gym<em>Fit</em>
            </a>

            <!-- Tabs -->
            <div class="auth-tabs">
                <a href="index.php?route=login" class="auth-tab active">Login</a>
                <a href="index.php?route=register" class="auth-tab">Register</a>
            </div>

            <h1 class="auth-title">Welcome Back</h1>
            <p class="auth-subtitle">
                Sign in to access your member space.
            </p>

            <?php if (isset($errors['login'])): ?>
                <div class="auth-error">
                    <?= htmlspecialchars($errors['login']) ?>
                </div>
            <?php endif; ?>

            <form action="index.php?route=check-login" method="POST">
                <input
                    type="hidden"
                    name="csrf-token"
                    value="<?= htmlspecialchars($_SESSION['csrf-token'] ?? '') ?>"
                >

                <div class="form-group">
                    <label>Email Address</label>
                    <input
                        type="email"
                        name="email"
                        placeholder="your@email.com"
                        value="<?= htmlspecialchars($old['email'] ?? '') ?>"
                    >
                    <?php if (isset($errors['email'])): ?>
                        <p class="error"><?= htmlspecialchars($errors['email']) ?></p>
                    <?php endif; ?>
                </div>

                <div class="form-group">
                    <label>Password</label>
                    <input
                        type="password"
                        name="password"
                        placeholder="••••••••"
                    >
                    <?php if (isset($errors['password'])): ?>
                        <p class="error"><?= htmlspecialchars($errors['password']) ?></p>
                    <?php endif; ?>
                </div>

                <button type="submit" class="btn-submit">
                    Sign In
                </button>
            </form>

            <div class="auth-secure">
                🔒 Secure login — GymFit Cairo
            </div>

            <div class="auth-footer-link">
                Not a member yet?
                <a href="index.php?route=register">Create an account</a>
            </div>

        </div>
    </div>
</div>

<script src="/gymfit/public/assets/js/main.js"></script>
</body>
</html>
<?php require_once __DIR__ . '/layout/footer.phtml'; ?>