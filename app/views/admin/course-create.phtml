<?php
$old    = $data['old']    ?? [];
$errors = $data['errors'] ?? [];
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>GymFit — Create Course</title>
    <link rel="stylesheet" href="public/assets/css/style.css">
</head>
<body>
<div class="layout">

    <div class="sidebar">
        <h2>GymFit</h2>
        <a href="index.php?route=admin">Dashboard</a>
        <a href="index.php?route=admin-members">Members</a>
        <a href="index.php?route=admin-courses" class="active">Courses</a>
        <a href="index.php?route=admin-subscriptions">Subscriptions</a>
        <hr style="border-color:#333;margin:20px 0;">
        <a href="index.php?route=logout">Logout</a>
    </div>

    <div class="main">
        <h1>Create Course</h1>
        <div class="card">
            <form action="index.php?route=admin-check-create-course" method="POST">

                <input type="hidden"
                       name="csrf-token"
                       value="<?= htmlspecialchars($_SESSION['csrf-token'] ?? '') ?>">

                <div class="form-group">
                    <label>Course Name *</label>
                    <input type="text" name="name"
                           value="<?= htmlspecialchars($old['name'] ?? '') ?>">
                    <?php if (isset($errors['name'])): ?>
                        <p class="error"><?= $errors['name'] ?></p>
                    <?php endif; ?>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label>Type</label>
                        <input type="text" name="type"
                               value="<?= htmlspecialchars($old['type'] ?? '') ?>"
                               placeholder="e.g. Cardio, Yoga, Dance">
                    </div>
                    <div class="form-group">
                        <label>Level</label>
                        <select name="level">
                            <?php foreach (['Beginner', 'Intermediate', 'Advanced'] as $level): ?>
                                <option value="<?= $level ?>"
                                        <?= (($old['level'] ?? 'Beginner') === $level) ? 'selected' : '' ?>>
                                    <?= $level ?>
                                </option>
                            <?php endforeach; ?>
                        </select>
                    </div>
                </div>

                <div class="form-group">
                    <label>Coach</label>
                    <input type="text" name="coach"
                           value="<?= htmlspecialchars($old['coach'] ?? '') ?>">
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label>Day *</label>
                        <select name="day">
                            <option value="">-- Select day --</option>
                            <?php foreach (['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'] as $day): ?>
                                <option value="<?= $day ?>"
                                        <?= (($old['day'] ?? '') === $day) ? 'selected' : '' ?>>
                                    <?= $day ?>
                                </option>
                            <?php endforeach; ?>
                        </select>
                        <?php if (isset($errors['day'])): ?>
                            <p class="error"><?= $errors['day'] ?></p>
                        <?php endif; ?>
                    </div>
                    <div class="form-group">
                        <label>Max Capacity</label>
                        <input type="number" name="max_capacity" min="1"
                               value="<?= htmlspecialchars($old['max_capacity'] ?? '20') ?>">
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label>Start Time *</label>
                        <input type="time" name="start_time"
                               value="<?= htmlspecialchars($old['start_time'] ?? '') ?>">
                        <?php if (isset($errors['start_time'])): ?>
                            <p class="error"><?= $errors['start_time'] ?></p>
                        <?php endif; ?>
                    </div>
                    <div class="form-group">
                        <label>End Time *</label>
                        <input type="time" name="end_time"
                               value="<?= htmlspecialchars($old['end_time'] ?? '') ?>">
                        <?php if (isset($errors['end_time'])): ?>
                            <p class="error"><?= $errors['end_time'] ?></p>
                        <?php endif; ?>
                    </div>
                </div>

                <button type="submit" class="btn-submit">Create Course</button>
            </form>
            <a href="index.php?route=admin-courses" class="btn-cancel">← Back to Courses</a>
        </div>
    </div>

</div>
</body>
</html><?php
$old    = $data['old']    ?? [];
$errors = $data['errors'] ?? [];
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>GymFit — Create Course</title>
    <link rel="stylesheet" href="public/assets/css/style.css">
</head>
<body>
<div class="layout">

    <div class="sidebar">
        <h2>GymFit</h2>
        <a href="index.php?route=admin">Dashboard</a>
        <a href="index.php?route=admin-members">Members</a>
        <a href="index.php?route=admin-courses" class="active">Courses</a>
        <a href="index.php?route=admin-subscriptions">Subscriptions</a>
        <hr style="border-color:#333;margin:20px 0;">
        <a href="index.php?route=logout">Logout</a>
    </div>

    <div class="main">
        <h1>Create Course</h1>
        <div class="card">
            <form action="index.php?route=admin-check-create-course" method="POST">

                <input type="hidden"
                       name="csrf-token"
                       value="<?= htmlspecialchars($_SESSION['csrf-token'] ?? '') ?>">

                <div class="form-group">
                    <label>Course Name *</label>
                    <input type="text" name="name"
                           value="<?= htmlspecialchars($old['name'] ?? '') ?>">
                    <?php if (isset($errors['name'])): ?>
                        <p class="error"><?= $errors['name'] ?></p>
                    <?php endif; ?>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label>Type</label>
                        <input type="text" name="type"
                               value="<?= htmlspecialchars($old['type'] ?? '') ?>"
                               placeholder="e.g. Cardio, Yoga, Dance">
                    </div>
                    <div class="form-group">
                        <label>Level</label>
                        <select name="level">
                            <?php foreach (['Beginner', 'Intermediate', 'Advanced'] as $level): ?>
                                <option value="<?= $level ?>"
                                        <?= (($old['level'] ?? 'Beginner') === $level) ? 'selected' : '' ?>>
                                    <?= $level ?>
                                </option>
                            <?php endforeach; ?>
                        </select>
                    </div>
                </div>

                <div class="form-group">
                    <label>Coach</label>
                    <input type="text" name="coach"
                           value="<?= htmlspecialchars($old['coach'] ?? '') ?>">
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label>Day *</label>
                        <select name="day">
                            <option value="">-- Select day --</option>
                            <?php foreach (['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'] as $day): ?>
                                <option value="<?= $day ?>"
                                        <?= (($old['day'] ?? '') === $day) ? 'selected' : '' ?>>
                                    <?= $day ?>
                                </option>
                            <?php endforeach; ?>
                        </select>
                        <?php if (isset($errors['day'])): ?>
                            <p class="error"><?= $errors['day'] ?></p>
                        <?php endif; ?>
                    </div>
                    <div class="form-group">
                        <label>Max Capacity</label>
                        <input type="number" name="max_capacity" min="1"
                               value="<?= htmlspecialchars($old['max_capacity'] ?? '20') ?>">
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label>Start Time *</label>
                        <input type="time" name="start_time"
                               value="<?= htmlspecialchars($old['start_time'] ?? '') ?>">
                        <?php if (isset($errors['start_time'])): ?>
                            <p class="error"><?= $errors['start_time'] ?></p>
                        <?php endif; ?>
                    </div>
                    <div class="form-group">
                        <label>End Time *</label>
                        <input type="time" name="end_time"
                               value="<?= htmlspecialchars($old['end_time'] ?? '') ?>">
                        <?php if (isset($errors['end_time'])): ?>
                            <p class="error"><?= $errors['end_time'] ?></p>
                        <?php endif; ?>
                    </div>
                </div>

                <button type="submit" class="btn-submit">Create Course</button>
            </form>
            <a href="index.php?route=admin-courses" class="btn-cancel">← Back to Courses</a>
        </div>
    </div>

</div>
</body>
</html>