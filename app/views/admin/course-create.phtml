<?php
$subscriptions = $data['subscriptions'] ?? [];
$old = $data['old'] ?? [];
$errors = $data['errors'] ?? [];
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GymFit — Create Account</title>
    <link rel="stylesheet" href="/gymfit/public/assets/css/style.css">
</head>
<body>

<div class="auth-page">

    <!-- LEFT — desktop only -->
    <div class="auth-left">
        <div class="auth-left-content">
            <p class="auth-left-title">
                Join GymFit<em>Start Today</em>
            </p>
            <ul class="auth-perks">
                <li>Choose from 3 membership plans</li>
                <li>Unlimited group classes (Premium+)</li>
                <li>Personal coaching sessions</li>
                <li>Sauna & hammam access (VIP)</li>
                <li>No registration fee</li>
            </ul>
        </div>
    </div>

    <!-- RIGHT — form -->
    <div class="auth-right">
        <div class="auth-box">

            <a href="index.php?route=home" class="auth-logo">
                Gym<em>Fit</em>
            </a>

            <div class="auth-tabs">
                <a href="index.php?route=login" class="auth-tab">Login</a>
                <a href="index.php?route=register" class="auth-tab active">Register</a>
            </div>

            <h1 class="auth-title">Create Account</h1>
            <p class="auth-subtitle">
                Join GymFit Cairo — Cairo's premium fitness center.
            </p>

            <form action="index.php?route=check-register" method="POST">
                <input
                    type="hidden"
                    name="csrf-token"
                    id="csrf-token"
                    value="<?= htmlspecialchars($_SESSION['csrf-token'] ?? '') ?>"
                />

                <div class="form-row">
                    <div class="form-group">
                        <label>First Name *</label>
                        <input
                            type="text"
                            name="first_name"
                            placeholder="Amira"
                            value="<?= htmlspecialchars($old['first_name'] ?? '') ?>"
                        >
                        <?php if (isset($errors['first_name'])): ?>
                            <p class="error"><?= htmlspecialchars($errors['first_name']) ?></p>
                        <?php endif; ?>
                    </div>

                    <div class="form-group">
                        <label>Last Name *</label>
                        <input
                            type="text"
                            name="last_name"
                            placeholder="Hassan"
                            value="<?= htmlspecialchars($old['last_name'] ?? '') ?>"
                        >
                        <?php if (isset($errors['last_name'])): ?>
                            <p class="error"><?= htmlspecialchars($errors['last_name']) ?></p>
                        <?php endif; ?>
                    </div>
                </div>

                <div class="form-group">
                    <label>Email Address *</label>
                    <input
                        type="email"
                        name="email"
                        placeholder="your@email.com"
                        value="<?= htmlspecialchars($old['email'] ?? '') ?>"
                    >
                    <?php if (isset($errors['email'])): ?>
                        <p class="error"><?= htmlspecialchars($errors['email']) ?></p>
                    <?php endif; ?>
                </div>

                <div class="form-group">
                    <label>Phone</label>
                    <input
                        type="text"
                        name="phone"
                        placeholder="+20 10 000 0000"
                        value="<?= htmlspecialchars($old['phone'] ?? '') ?>"
                    >
                    <?php if (isset($errors['phone'])): ?>
                        <p class="error"><?= htmlspecialchars($errors['phone']) ?></p>
                    <?php endif; ?>
                </div>

                <fieldset class="form-group">
                    <legend>Choose Your Plan *</legend>

                    <div class="plan-selector">
                        <?php foreach ($subscriptions as $subscription): ?>
                            <label
                                class="plan-option <?= (($old['id_subscription'] ?? '') == $subscription->getId()) ? 'selected' : '' ?>"
                                id="plan-label-<?= htmlspecialchars((string) $subscription->getId()) ?>"
                            >
                                <input
                                    type="radio"
                                    name="id_subscription"
                                    value="<?= htmlspecialchars((string) $subscription->getId()) ?>"
                                    <?= (($old['id_subscription'] ?? '') == $subscription->getId()) ? 'checked' : '' ?>
                                    onchange="selectPlan(this)"
                                >
                                <span class="plan-option-name">
                                    <?= htmlspecialchars($subscription->getName()) ?>
                                </span>
                                <span class="plan-option-price">
                                    €<?= htmlspecialchars((string) $subscription->getMonthlyPrice()) ?>/mo
                                </span>
                            </label>
                        <?php endforeach; ?>
                    </div>

                    <?php if (isset($errors['id_subscription'])): ?>
                        <p class="error"><?= htmlspecialchars($errors['id_subscription']) ?></p>
                    <?php endif; ?>
                </fieldset>

                <div class="form-group">
                    <label>Password *</label>
                    <input
                        type="password"
                        name="password"
                        id="password-input"
                        placeholder="Minimum 8 characters"
                        oninput="checkPasswordStrength(this.value)"
                    >
                    <div class="pwd-strength">
                        <div class="pwd-bar" id="bar1"></div>
                        <div class="pwd-bar" id="bar2"></div>
                        <div class="pwd-bar" id="bar3"></div>
                        <div class="pwd-bar" id="bar4"></div>
                    </div>
                    <div class="pwd-label" id="pwd-label">
                        Minimum 8 characters
                    </div>
                    <?php if (isset($errors['password'])): ?>
                        <p class="error"><?= htmlspecialchars($errors['password']) ?></p>
                    <?php endif; ?>
                </div>

                <div class="form-group">
                    <label>Confirm Password *</label>
                    <input
                        type="password"
                        name="confirm_password"
                        placeholder="Repeat your password"
                    >
                    <?php if (isset($errors['confirm_password'])): ?>
                        <p class="error"><?= htmlspecialchars($errors['confirm_password']) ?></p>
                    <?php endif; ?>
                </div>

                <div class="form-group auth-cgu">
                    <label class="cgu-label">
                        <input type="checkbox" name="cgu" required>
                        I agree to the
                        <a href="#">Terms of Use</a>
                        and
                        <a href="#">Privacy Policy</a>
                        of GymFit.
                    </label>
                </div>

                <button type="submit" class="btn-submit">
                    Create My Account
                </button>
            </form>

            <div class="auth-secure">
                🔒 Secure Sign-Up — No credit card required
            </div>

            <div class="auth-footer-link">
                Already a member?
                <a href="index.php?route=login">Sign In</a>
            </div>

        </div>
    </div>
</div>

<script src="/gymfit/public/assets/js/main.js"></script>
</body>
</html>