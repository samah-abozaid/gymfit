<?php
$subscriptions = $data['subscriptions'] ?? [];
$success       = $data['success']       ?? null;
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>GymFit — Subscriptions</title>
    <link rel="stylesheet" href="public/assets/css/style.css">
</head>
<body>
<div class="layout">

    <div class="sidebar">
        <h2>GymFit</h2>
        <a href="index.php?route=admin">Dashboard</a>
        <a href="index.php?route=admin-members">Members</a>
        <a href="index.php?route=admin-courses">Courses</a>
        <a href="index.php?route=admin-subscriptions" class="active">Subscriptions</a>
        <hr style="border-color:#333;margin:20px 0;">
        <a href="index.php?route=logout">Logout</a>
    </div>

    <div class="main">
        <h1>Subscription Plans</h1>

        <?php if ($success): ?>
            <div class="alert-success"><?= htmlspecialchars($success) ?></div>
        <?php endif; ?>

        <div class="card">
            <div class="card-header">
                <h2>All Plans (<?= count($subscriptions) ?>)</h2>
                <a href="index.php?route=admin-create-subscription" class="btn btn-add">+ Add Plan</a>
            </div>
            <table>
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Name</th>
                        <th>Price / mo</th>
                        <th>Classes</th>
                        <th>Coaching</th>
                        <th>Sauna</th>
                        <th>Description</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($subscriptions as $sub): ?>
                    <tr>
                        <td><?= $sub->getId() ?></td>
                        <td><strong><?= htmlspecialchars($sub->getName()) ?></strong></td>
                        <td>€<?= number_format($sub->getMonthlyPrice(), 0) ?></td>
                        <td><?= $sub->hasClassAccess()    ? '<span class="badge badge-active">✓</span>' : '<span style="color:#555">✗</span>' ?></td>
                        <td><?= $sub->hasCoachingAccess() ? '<span class="badge badge-active">✓</span>' : '<span style="color:#555">✗</span>' ?></td>
                        <td><?= $sub->hasSaunaAccess()    ? '<span class="badge badge-active">✓</span>' : '<span style="color:#555">✗</span>' ?></td>
                        <td style="color:#888;font-size:13px"><?= htmlspecialchars($sub->getDescription() ?? '—') ?></td>
                        <td>
                            <a href="index.php?route=admin-update-subscription&id=<?= $sub->getId() ?>"
                               class="btn btn-edit">Edit</a>
                            <a href="index.php?route=admin-delete-subscription&id=<?= $sub->getId() ?>"
                               class="btn btn-delete"
                               onclick="return confirm('Delete plan <?= htmlspecialchars($sub->getName()) ?>?')">
                               Delete
                            </a>
                        </td>
                    </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>

</div>
</body>
</html>
