<?php foreach ($courses as $course): ?>
    <p>
        <strong><?= htmlspecialchars($course->getName()) ?></strong>
        — <?= htmlspecialchars($course->getDay()) ?>
        — <?= htmlspecialchars($course->getStartTime()) ?>
        — Coach : <?= htmlspecialchars($course->getCoach()) ?>
    </p>
<?php endforeach; ?>

<?php foreach ($subscriptions as $subscription): ?>
    <p>
        <strong><?= htmlspecialchars($subscription->getName()) ?></strong>
        — <?= $subscription->getMonthlyPrice() ?>€/mois
    </p>
<?php endforeach; ?>

<?php if (isset($_SESSION['user'])): ?>
    <p style="color:green;">
        ✅ Connecté : <?= $_SESSION['user']['first_name'] ?>
        <?= $_SESSION['user']['last_name'] ?>
    </p>
    <a href="index.php?route=logout">Logout</a>
<?php else: ?>
    <p>Non connecté</p>
    <a href="index.php?route=login">Login</a>
<?php endif; ?>